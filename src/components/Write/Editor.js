import template from '../../templates/Write/editor.template';
import Component from '../../core/component';

export default class Editor extends Component {
  constructor(data) {
    super(template, data);
  }
}
